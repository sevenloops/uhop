[pytest]
markers =
    perf: performance-intensive benchmarks (skipped by default)
    gpu: requires a GPU/OpenCL runtime (skipped if unavailable)
testpaths = tests
norecursedirs = .git .venv venv env frontend backend docs examples .pytest_cache .ruff_cache
addopts = -ra
